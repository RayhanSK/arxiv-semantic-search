# ---- core (always required) ----
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.35
requests>=2.31
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
rank-bm25>=0.2.2
faiss-cpu>=1.8.0
pymupdf>=1.24
numpy>=1.26
pytest>=8.0

# ---- production ML stack (recommended; see requirements-ml.txt) ----
# Installs torch. Enables real semantic embeddings, cross-encoder
# reranking and local LLM generation. Without it, the system runs with
# deterministic lexical fallbacks (useful for CI / low-resource machines).
